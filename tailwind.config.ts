// Tailwind CSS v4 uses CSS-first configuration via the @theme directive in your
// CSS (see app/globals.css). Keeping this file empty ensures the PostCSS plugin
// runs without conflicting legacy config.
export default {};
